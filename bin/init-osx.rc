# more specifically, set vars for our preferred local postgres configuration +
# one important setting (prefixed by DYLD) to keep shared libraries from crashing
export PGROOT=/opt/pg9
export PGDATA=$PGROOT/data
export $PGROOT/bin:$PATH
export DYLD_FALLBACK_LIBRARY_PATH=$PGROOT/lib
